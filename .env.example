# Full URL of the oracle-of-inclusion-api Cloudflare Worker.
# The app will POST { question } to this URL and expect { answer } in return.
VITE_ORACLE_API_URL=https://your-worker.your-subdomain.workers.dev
